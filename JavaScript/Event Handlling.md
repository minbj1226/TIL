# JavaScript Event Handlling
- Inline 방식: HTML 태그의 onxxx속성으로 이벤트를 발생시키고 처리하는 방식
``<input type="button" onclick="함수명()">`` <- HTML태그와 JS코드가 합쳐진다.

- 이벤트를 등록하여 처리: 익명함수(anonymous function)사용

문법)
``document.getElementById("아이디").onxxx``